apiVersion: v1
kind: ConfigMap
metadata:
  name: musicshop-config
  namespace: musicshop-prod
data:
  # Inserisci qui l'endpoint del tuo RDS su AWS
  DB_HOST: "musicshop-db.cxxxxxxx.eu-west-1.rds.amazonaws.com"
  DB_PORT: "5432"
  DB_NAME: "musicshop"
